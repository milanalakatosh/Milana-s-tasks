//Удалите одной регуляркой все слова из предложения, содержащие две одинаковые следующие друг за другом буквы.
const sentence = document.querySelector(".sentence");

// sentence.addEventListener("input", () => {
//     const regexToFindWords = new RegExp("\\b\\w{2,}\\b", "gi");
//     //const words = sentence.value.match(regexToFindWords));
// });
sentence.addEventListener("input", () => {
    const regex = / /gi;
}

